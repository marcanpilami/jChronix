Frequent shell runner plugin
#####################################

This is a plugin for the "command agent" which adds the agent the ability for local execution of commands for common shells:

* bash
* ksh
* sh
* powershell (windows)
* cmd.exe (windows)

